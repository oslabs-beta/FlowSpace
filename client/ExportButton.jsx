import React, { useState, useEffect } from "react";

const ExportButton = (props) => {

    return (
        <a href="/export" style={{ paddingLeft: "73%" }}>Click to download loss JSON file</a>
    )
}

export default ExportButton;