import React from 'react';
import GraphIcon from '../assets/verticalNav-graph.png';

// component for the graph icon on the tab component
export default function GraphIconComponent() {
  return (
    <img src={GraphIcon} style={{height: 'auto', width: '50%', marginLeft: '-1.5rem'}}></img> 
  )
}